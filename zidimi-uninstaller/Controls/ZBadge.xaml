<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:zidimi_uninstaller.Controls">
    <Style TargetType="{x:Type controls:ZBadge}">
        <Setter Property="Foreground" Value="{StaticResource ZBrushDarkGray}"/>
        <Setter Property="Background" Value="{StaticResource ZBrushSurfaceMuted}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ZBrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="9,4"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ZBadge}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter ContentSource="Content"
                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Variant" Value="Accent">
                <Setter Property="Background" Value="{StaticResource ZBrushAccentTint}"/>
                <Setter Property="BorderBrush" Value="#40EA0232"/>
                <Setter Property="Foreground" Value="{StaticResource ZBrushAccent}"/>
            </Trigger>
            <Trigger Property="Variant" Value="Danger">
                <Setter Property="Background" Value="#FDE5E9"/>
                <Setter Property="BorderBrush" Value="#E8BCC4"/>
                <Setter Property="Foreground" Value="{StaticResource ZBrushBrown}"/>
            </Trigger>
            <Trigger Property="Variant" Value="Info">
                <Setter Property="Background" Value="#EAF3FB"/>
                <Setter Property="BorderBrush" Value="#C7DDF2"/>
                <Setter Property="Foreground" Value="#0B5CAD"/>
            </Trigger>
            <Trigger Property="Variant" Value="Success">
                <Setter Property="Background" Value="#E8F5EC"/>
                <Setter Property="BorderBrush" Value="#C8E5D1"/>
                <Setter Property="Foreground" Value="#1E7A3C"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
