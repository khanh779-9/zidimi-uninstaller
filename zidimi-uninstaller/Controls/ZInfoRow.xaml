<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:zidimi_uninstaller.Controls"
>

    <!-- ============ ZInfoRow ============ -->
    <Style TargetType="{x:Type controls:ZInfoRow}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ZInfoRow}">
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{TemplateBinding Label}" Foreground="{StaticResource ZBrushGray}" FontSize="12.5" VerticalAlignment="Top"/>
                        <TextBlock x:Name="ValueText" Grid.Column="1" Text="{TemplateBinding Value}"
                                   Foreground="{StaticResource ZBrushBlack}" FontSize="12.5" VerticalAlignment="Top"
                                   TextWrapping="Wrap" Margin="12,0,0,0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsAccent" Value="True">
                            <Setter TargetName="ValueText" Property="Foreground" Value="{StaticResource ZBrushAccent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

