<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:zidimi_uninstaller.Controls"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
                    xmlns:sys="clr-namespace:System.Windows;assembly=PresentationFramework"
>

    <!-- ZWindow buttons -->
    <Style x:Key="ZWindowBarButton" TargetType="{x:Type Button}">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Foreground" Value="{StaticResource ZBrushLighterGray}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#3D525153"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#52FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ZWindowCloseButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ZWindowBarButton}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ZBrushAccent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ZWindow - window chrome -->
    <Style x:Key="ZWindowStyle" TargetType="{x:Type controls:ZWindow}">
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="48"
                                    ResizeBorderThickness="6"
                                    GlassFrameThickness="0"
                                    CornerRadius="0"
                                    UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
        <Setter Property="Background" Value="{StaticResource ZBrushWhite}"/>
        <Setter Property="Foreground" Value="{StaticResource ZBrushBlack}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ZWindow}">
                    <Grid Background="Transparent">
                        <Border x:Name="ChromeBorder"
                                Background="{StaticResource ZBrushWhite}"
                                BorderBrush="{StaticResource ZBrushLightGray}"
                                BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="48"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Title bar -->
                                <Border Grid.Row="0" Background="{StaticResource ZBrushDark}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                                            <Image Source="/zidimi-icon.ico" Height="24" Width="24"/>
                                            <!--<TextBlock Text="Zidimi" Foreground="White" FontWeight="SemiBold" FontSize="14"
                                                       Margin="10,0,0,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Uninstaller" Foreground="{StaticResource ZBrushAccentLight}" FontSize="12"
                                                       Margin="6,0,0,0" VerticalAlignment="Center"/>-->
                                        </StackPanel>

                                        <TextBlock Grid.Column="1" Text="{TemplateBinding Title}"
                                                   Foreground="{StaticResource ZBrushLighterGray}" FontSize="16"
                                                   VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0"
                                                   TextTrimming="CharacterEllipsis"/>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Stretch">
                                            <Button Style="{StaticResource ZWindowBarButton}"
                                                    Command="{x:Static sys:SystemCommands.MinimizeWindowCommand}"
                                                    CommandTarget="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                                <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                            <Button x:Name="MaximizeButton" Style="{StaticResource ZWindowBarButton}"
                                                    Command="{x:Static sys:SystemCommands.MaximizeWindowCommand}"
                                                    CommandTarget="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                                <TextBlock Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                            <Button x:Name="RestoreButton" Style="{StaticResource ZWindowBarButton}" Visibility="Collapsed"
                                                    Command="{x:Static sys:SystemCommands.RestoreWindowCommand}"
                                                    CommandTarget="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                                <TextBlock Text="&#xE923;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                            <Button Style="{StaticResource ZWindowCloseButton}"
                                                    Command="{x:Static sys:SystemCommands.CloseWindowCommand}"
                                                    CommandTarget="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                                <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Content -->
                                <ContentPresenter Grid.Row="1" Content="{TemplateBinding Content}"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="RestoreButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ChromeBorder" Property="BorderThickness" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

